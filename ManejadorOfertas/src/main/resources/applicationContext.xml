<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:c="http://www.springframework.org/schema/c"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:p="http://www.springframework.org/schema/p"

       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
          http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.0.xsd
          http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
          http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit-1.0.xsd">

    <context:component-scan base-package="edu.eci.arsw.exam"/>

    <rabbit:connection-factory id="connectionFactory" host="localhost" username="guest" password="guest" />
    <rabbit:admin connection-factory="connectionFactory" />

    <rabbit:queue name="productos.queue" id="productosQueue" durable="false"/>
    <rabbit:queue name="ofertas.queue" id="ofertasQueue" durable="false"/>

    <rabbit:direct-exchange name="QDM-EXCHANGE" id="qdmExchange" durable="false">
        <rabbit:bindings>
            <rabbit:binding queue="productosQueue" key="productos"/>
            <rabbit:binding queue="ofertasQueue" key="ofertas"/>
        </rabbit:bindings>
    </rabbit:direct-exchange>

    <rabbit:template id="producerTemplate" connection-factory="connectionFactory" exchange="QDM-EXCHANGE"/>

    <bean id="producer" class="edu.eci.arsw.exam.events.OffertMessageProducer">
        <property name="amqpTemplate" ref="producerTemplate"/>
    </bean>

    <bean id="mainFrame" class="edu.eci.arsw.exam.MainFrame">
        <property name="messageProducer" ref="producer"/>
        <property name="fachadaPersistenciaOfertas" ref="fachadaPersistenciaOfertas"/>
    </bean>

    <bean id="fachadaPersistenciaOfertas" class="edu.eci.arsw.exam.FachadaPersistenciaOfertas" />

    <rabbit:direct-exchange id="winnerExchange" name="WINNER-EXCHANGE" durable="false">
    </rabbit:direct-exchange>

    <bean id="ofertaListener" class="edu.eci.arsw.exam.events.OfertaListener">
        <property name="fachada" ref="fachadaPersistenciaOfertas"/>
        <property name="rabbitTemplate" ref="winnerTemplate"/>
    </bean>

    <rabbit:listener-container connection-factory="connectionFactory">
        <rabbit:listener ref="ofertaListener" queues="ofertas.queue" method="recibirOferta"/>
    </rabbit:listener-container>

    <rabbit:template id="winnerTemplate" connection-factory="connectionFactory" exchange="WINNER-EXCHANGE"/>

    <rabbit:listener-container connection-factory="connectionFactory">
        <rabbit:listener ref="ofertaListener" queues="ofertas.queue"/>
    </rabbit:listener-container>

</beans>